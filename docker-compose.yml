version: '3'
services:
  api:
    build: .
    ports:
      - "3001:3001"
      - "25565:25565"
      - "3002:3002"
    volumes:
      - ./server:/app/server
      - ./data:/app/data
    env_file:
      - .env
    restart: always
    command: >
      sh -c "npm install && node index.js"